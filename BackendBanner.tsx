import React from 'react';

// This is a placeholder component to prevent the application from crashing
// due to an empty file being imported. It can be implemented later if needed.
const BackendBanner: React.FC = () => {
  return null; // Render nothing for now
};

export default BackendBanner;
